Small count or neutral pill — use for list counts and notification numbers, not test outcomes (use StatusBadge for those).

```jsx
<Tabs> Tests <Badge>128</Badge> </Tabs>
<Badge variant="brand">12 new</Badge>
<Badge variant="accent">3 flaky</Badge>
```

- **variant**: `neutral` (default), `brand`, `accent`, `solid`.
