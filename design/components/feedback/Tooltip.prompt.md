Hover/focus tooltip for icon buttons and truncated values.

```jsx
<Tooltip content="Re-run this test" kbd="⌘R">
  <IconButton label="Re-run"><RefreshIcon/></IconButton>
</Tooltip>
```

- Wraps exactly one trigger. Appears above on hover or keyboard focus.
- **kbd** renders a muted shortcut hint on the right.
