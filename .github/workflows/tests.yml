name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality + package integrity
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install quality tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install ".[dev]" build twine ruff
      - name: Catch syntax and import-name mistakes
        run: |
          python -m compileall -q argus .github/scripts packaging
          ruff check argus tests .github/scripts packaging --select E9,F63,F7,F82
      - name: Build and verify Python distribution
        run: |
          python -m build
          python -m twine check dist/*

  pytest:
    name: pytest · ${{ matrix.label }} · py${{ matrix.python }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Linux x64
            os: ubuntu-24.04
            python: "3.10"
            install_args: .[dev,linux]
          - label: Linux x64
            os: ubuntu-24.04
            python: "3.12"
            install_args: .[dev,linux]
          - label: Linux ARM64
            os: ubuntu-24.04-arm
            python: "3.12"
            install_args: .[dev,linux]
          - label: Windows x64
            os: windows-2025
            python: "3.10"
            install_args: .[dev,windows]
          - label: Windows x64
            os: windows-2025
            python: "3.12"
            install_args: .[dev,windows]
          - label: Windows ARM64
            os: windows-11-arm
            python: "3.12"
            install_args: .[dev,windows]
          - label: macOS Apple Silicon
            os: macos-15
            python: "3.12"
            install_args: .[dev]
          - label: macOS Intel
            os: macos-15-intel
            python: "3.12"
            install_args: .[dev]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}
          cache: pip
      - name: Install test dependencies
        if: matrix.label != 'Windows ARM64'
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e "${{ matrix.install_args }}"
      - name: Install test dependencies (Windows ARM64)
        if: matrix.label == 'Windows ARM64'
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e "${{ matrix.install_args }}" "cryptography==46.0.3"
      - name: Run tests
        run: python -m pytest -q
